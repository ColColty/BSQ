language: c

compiler: gcc

notification:
  email:
    recipients:
      - tomas.forne-cappeau@epitech.eu
    on_success: never
    on_failure: always

script:
  - git clone --recursive https://github.com/Snaipe/Criterion
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
  - sudo make install
  - make && ./bsq mouli_maps/testing_map